name: vet_mobile_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  # Firebase packages removed temporarily for development
  # firebase_core: ^2.15.1
  # firebase_auth: ^4.7.3
  # cloud_firestore: ^4.8.5
  url_launcher: ^6.2.2
  http: ^1.1.2
  cached_network_image: ^3.3.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  flutter_svg: ^2.0.7  # Added for SVG support

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
